import 'package:flutter/material.dart';

class AppColors {
  // Primary Palette - Calm and focused
  static const Color primary = Color(0xFF2D5A27); // Forest Green
  static const Color secondary = Color(0xFFE8F5E9); // Light Mint
  static const Color accent = Color(0xFF81C784); // Soft Green

  // States
  static const Color calm = Color(0xFF66BB6A);
  static const Color strained = Color(0xFFFFA726);
  static const Color fatigued = Color(0xFFEF5350);

  // Neutral
  static const Color background = Color(0xFFFFF9F0); // Paper-like cream
  static const Color surface = Colors.white;
  static const Color textBody = Color(0xFF2C3E50);
  static const Color textDim = Color(0xFF7F8C8D);
}
