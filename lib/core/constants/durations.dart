class AppDurations {
  static const Duration focusSprint = Duration(minutes: 90);
  static const Duration recoverySession = Duration(minutes: 15);
  static const Duration microIntervention = Duration(seconds: 60);
}
